using System;

namespace GoopGame
{
#if WINDOWS || XBOX
    static class Program
    {
        /// <summary>
        /// The main entry point for the application.
        /// </summary>
        static void Main(string[] args)
        {
            using (GoopGame game = new GoopGame())
            {
                game.Run();
            }
        }
    }
#endif
}

